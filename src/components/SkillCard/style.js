import styled from 'styled-components';

export const Container = styled.figure`
    width: 10.2rem;
    height: 7.7rem;

    display: flex;
    align-items: center;
    justify-content: center;
    
    background: #1E1E1E;
    border-radius: 8px;

    padding: 1.5rem;
    object-fit: cover;
    img{
        object-fit: cover;
    }
`